; Installed by radio-website into /etc/php/<ver>/fpm/pool.d/zz-radio.conf.
; Overrides the stock www pool sizing: the Pi Zero has 512 MB shared with
; the radio daemon, and this site has exactly one user at a time.
[www]
pm = static
pm.max_children = 2
